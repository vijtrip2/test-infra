periodics:
- interval: 60m
  agent: kubernetes
  name: echo-test
  spec:
    serviceAccountName: pre-submit-service-account
    containers:
    - image: alpine
      command: ["/bin/date"]

postsubmits:
  vijtrip2/test-infra:
  - name: push-prow-test
    decorate: true
    spec:
      serviceAccountName: post-submit-service-account
      containers:
        - image: alpine
          command: ["/bin/sh"]
          args: ["-c", "ls -al /"]
    branches:
    - ^local-prow-test$